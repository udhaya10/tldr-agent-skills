#!/usr/bin/env bash
# OMITTED per 05_OMITTED_COMMANDS_RATIONALE.md: `tldr cfg` is not exposed as a
# CLI subcommand. The engine is reachable only via `slice`, `reaching-defs`, or `chop`.
echo "Omitted: tldr cfg is not a top-level CLI subcommand." >&2
exit 1
